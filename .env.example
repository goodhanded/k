# .env.example - Configuration for project k
# Copy this file to .env and set appropriate values for local development.

# K_PATH is the root directory of the project. If set, the following paths will be derived:
#   ENV_PATH = <K_PATH>/.env
#   COMMANDS_YAML_PATH = <K_PATH>/commands.yaml
#   SERVICES_YAML_PATH = <K_PATH>/services.yaml

# AWS configuration for Amazon Transcribe
TRANSCRIPTS_BUCKET=your-transcripts-bucket-name
AWS_PATH=/usr/local/bin/aws

# Paths for various services
PROMPT_TEMPLATE_PATH=path/to/your/prompt/templates
TRANSCRIPTS_BASE_PATH=path/to/your/transcripts
OBSIDIAN_VAULT_PATH=path/to/your/obsidian/vault
FAISS_INDEX_PATH=path/to/your/faiss/index

# Optional: Calendar folder used by ObsidianVault (default is "Calendar")
OBSIDIAN_CALENDAR_FOLDER=Calendar

# OpenAI API key for services using OpenAI
OPENAI_API_KEY=your-openai-api-key

# Discord Bot token (if using Discord bot functionality)
DISCORD_BOT_TOKEN=your-discord-bot-token

# Model for Generate Changeset Node (used by ChatOpenAI)
GENERATE_CHANGESET_MODEL=o3-mini

# Model for Generate Code Advice Node (used by ChatOpenAI)
GENERATE_CODE_ADVICE_MODEL=o3-mini

# Model for Generate User Stories Node (used by ChatOpenAI)
GENERATE_USER_STORIES_MODEL=o3-mini